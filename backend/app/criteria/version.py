"""Criteria engine version.

Stamped on every processing session. Part of the six reproducibility inputs in
spec section 43: original file + selections + corrections + snapshot + this.
Bump it whenever a change could alter the verdict a rule produces.
"""

ENGINE_VERSION = "1.0.0"
